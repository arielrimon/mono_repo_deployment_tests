name: Deploy Cloud Functions

on:
  push:
    branches:
      - main  # Deploy on push to the main branch

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.9'

    - name: Authenticate to Google Cloud
      uses: google-github-actions/auth@v1
      with:
        credentials_json: '${{ secrets.GCLOUD_SA_KEY }}'

    - name: Set up gcloud CLI
      uses: google-github-actions/setup-gcloud@v1
      with:
        project_id: ${{ secrets.GCLOUD_PROJECT_ID }}
        service_account_key: ${{ secrets.GCLOUD_SA_KEY }}

    # Deploy Service 1
    - name: Deploy Service 1 Function
      run: |
        cd service1  # Navigate to service1 directory
        gcloud functions deploy service1_function \
          --runtime python39 \
          --trigger-http \
          --allow-unauthenticated

    # Deploy Service 2
    - name: Deploy Service 2 Function
      run: |
        cd service2  # Navigate to service2 directory
        gcloud functions deploy service2_function \
          --runtime python39 \
          --trigger-http \
          --allow-unauthenticated
